{{questionDesc}}

import std/[
  algorithm,
  bitops,
  deques,
  heapqueue,
  jsconsole,
  jsre,
  math,
  macros,
  sequtils,
  strformat,
  strutils,
  sugar,
]

type
  ListNode = ref object
    val: cdouble
    next: ListNode

  TreeNode = ref object
    val: cdouble
    left, right: TreeNode

  NaryTreeNode = ref object
    val: cdouble
    children: seq[NaryTreeNode]

  GraphNode = ref object
    val: cdouble
    neighbors: seq[GraphNode]

proc {{functionName}}({{parameters}}): {{returnType}} {.exportc.} =
  {{defaultReturnValue}}
