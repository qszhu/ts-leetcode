{{questionDesc}}

import std/[
  algorithm,
  bitops,
  cstrutils,
  deques,
  heapqueue,
  jsbigints,
  jsconsole,
  jsre,
  math,
  macros,
  sequtils,
  strformat,
  strutils,
  sugar,
]

import types

proc {{functionName}}({{parameters}}): {{returnType}} {.exportc.} =
  {{defaultReturnValue}}
