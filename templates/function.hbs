{{questionDesc}}

import std/[algorithm, bitops, complex, cstrutils, deques, heapqueue, math, macros, options, parseutils, sequtils, strformat, strutils, sugar]
import std/[jsbigints, jsconsole, jsre]

import types

proc {{functionName}}({{parameters}}): {{returnType}} {.exportc.} =
  {{defaultReturnValue}}
