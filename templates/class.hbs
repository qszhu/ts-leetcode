{{questionDesc}}

import std/[
  algorithm,
  bitops,
  cstrutils,
  deques,
  heapqueue,
  jsconsole,
  jsre,
  math,
  macros,
  sequtils,
  strformat,
  strutils,
  sugar,
]

type {{className}} = ref object

{{#if constructor}}
proc create{{className}}({{constructor.params}}): {{className}} {.exportc: "{{className}}".} =
  var this {.importc, nodecl.}: {{className}}
  this
{{/if}}

{{#each methods}}
{.emit: "{{className}}.prototype.{{name}} = {{name}}".}
proc {{name}}({{params}}): {{returnType}} {.exportc.} =
  var this {.importc, nodecl.}: {{className}}
  {{defaultReturnValue}}

{{/each}}
