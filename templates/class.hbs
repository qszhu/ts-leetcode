{{questionDesc}}

import
  algorithm,
  jsre,
  math,
  macros,
  sequtils,
  sets,
  strformat,
  strutils,
  sugar,
  tables

type
  {{className}} = object

{{#if constructor}}
proc {{className}}({{constructor.params}}) =
  discard
{{/if}}

{{#each methods}}
proc {{this.name}}({{this.params}}): {{this.returnType}} {.exportc.} =
  {{this.defaultReturnValue}}
{{/each}}
