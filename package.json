{
  "name": "tsoj",
  "version": "0.2.0",
  "scripts": {
    "init": "ts-node -r dotenv-safe/config scripts/init.ts",
    "select": "ts-node -r dotenv-safe/config scripts/select.ts",
    "build": "ts-node -r dotenv-safe/config scripts/build.ts",
    "build-all": "ts-node -r dotenv-safe/config scripts/build-all.ts",
    "test": "npm run build && ts-node -r dotenv-safe/config scripts/test.ts",
    "test-skipbuild": "ts-node -r dotenv-safe/config scripts/test.ts",
    "test-all": "ts-node -r dotenv-safe/config scripts/test-all.ts",
    "submit": "ts-node -r dotenv-safe/config scripts/submit.ts"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "dotenv-safe": "^8.2.0",
    "globby": "^11.0.1",
    "handlebars": "^4.7.6",
    "javascript-obfuscator": "^1.9.0",
    "nodemon": "^2.0.4",
    "shelljs": "^0.8.4",
    "terser": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^14.0.27",
    "@types/shelljs": "^0.8.8",
    "@typescript-eslint/eslint-plugin": "^3.8.0",
    "@typescript-eslint/parser": "^3.8.0",
    "eslint": "^7.6.0",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.7"
  }
}
